const toast=m=>{const t=document.getElementById("toast");t.textContent=m;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),2500);};
document.querySelectorAll(".show-password").forEach(b=>b.onclick=()=>{const i=b.previousElementSibling;i.type=i.type==="password"?"text":"password";b.textContent=i.type==="password"?"👁️":"🙈";});
const signupForm=document.getElementById("signupForm");
if(signupForm)signupForm.addEventListener("submit",e=>{e.preventDefault();const name=signupName.value.trim(),phone=signupPhone.value.replace(/\s/g,""),password=signupPassword.value,confirm=signupConfirm.value;if(password!==confirm){toast("Les mots de passe ne correspondent pas.");return;}const users=JSON.parse(localStorage.getItem("niougalUsers")||"[]");if(users.some(u=>u.phone===phone)){toast("Un compte existe déjà avec ce numéro.");return;}const user={id:"USR-"+Date.now().toString().slice(-6),name,phone,password,createdAt:new Date().toISOString()};users.push(user);localStorage.setItem("niougalUsers",JSON.stringify(users));localStorage.setItem("niougalCurrentUser",JSON.stringify({id:user.id,name:user.name,phone:user.phone}));toast("Compte créé avec succès !");setTimeout(()=>location.href="catalogue.html",700);});
const loginForm=document.getElementById("loginForm");
if(loginForm)loginForm.addEventListener("submit",e=>{e.preventDefault();const phone=loginPhone.value.replace(/\s/g,""),password=loginPassword.value;const users=JSON.parse(localStorage.getItem("niougalUsers")||"[]");const user=users.find(u=>u.phone===phone&&u.password===password);if(!user){toast("Numéro ou mot de passe incorrect.");return;}localStorage.setItem("niougalCurrentUser",JSON.stringify({id:user.id,name:user.name,phone:user.phone}));toast("Connexion réussie !");setTimeout(()=>location.href="catalogue.html",600);});